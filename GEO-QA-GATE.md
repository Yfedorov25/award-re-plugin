# GEO-QA GATE — закон для всіх Хігсфілд-генерацій з існуючого кадру

Мета: жодна перегенерація (relight день/ніч, i2i re-skin, keyframe для відео) не потрапляє
в `public/assets` сайту, поки не пройшла гейт геометрії. Перспектива, вікна, дах,
членування фасаду мають збігатися з еталоном 1-в-1.

## Пайплайн (обов'язковий, крок за кроком)

```
1. REF   = оригінальний рендер архітектора (правда).
2. GEN   = вихід Хігсфілд (nano_banana_pro 4K, KEEP-EXACT у промпті).
3. GATE  = scripts/geo-qa.sh REF GEN out/ label
           → edge-diff скор + overlay (червоне=REF, зелене=GEN, жовте=збіг).
4. ОЧІ   = overlay дивитись ЗАВЖДИ, навіть при PASS (скор — порадник, не суддя).
5. ПОКАЗ = пара REF+GEN (+overlay) у артефакт «було→стало» → валідація юзера.
6. Тільки після 5 — конвертація в webp і заміна на сайті (НОВЕ ім'я файлу, immutable-кеш).
```

## Каліброві точки (smarts, 07/2026)

| Кейс | Скор | Значення |
|---|---|---|
| Ідентичні кадри | 0 | нуль |
| Чесний relight день→ніч (схвалені пари інтер'єрів) | 3.2–4.5 | PASS-band |
| Та сама камера, реально змінений фасад (стара→нова комерція) | 8.6 | FAIL має ловити |
| Інший ракурс | 27.5 | грубий провал |

Пороги: **PASS ≤ 4.5 · REVIEW 4.5–7 · FAIL > 7**.

## ЕКСТЕР'ЄР-ПРОТОКОЛ (обов'язково для фасадів)

Повна сцена шумить (листя, хмари, машини, фальцеві ребра даху = муар в edge-мапі,
відбиття у склі). Тому для фасадів:

1. `GEOQA_CROP="iw*0.93:ih*0.48:iw*0.04:ih*0.26"` (частки підганяти під кадр) — тільки будівля;
2. порівняння на спільній ширині (скрипт робить сам);
3. МАКРО-режим: `gblur=sigma=2.5` перед edgedetect (`low=0.1:high=0.25`) — тільки великі контури.
   Калібр (smarts render_02, 07/2026): self=0 · чесний 4K relight=5.8–6.0 · реальна зміна фасаду=8.3.
   Екстер'єр-пороги (макро): **PASS ≤ 6.5 · REVIEW 6.5–7.5 · FAIL > 7.5**.
4. Скор — порадник. Вирішують: overlay + порівняльні смуги (REF↑/GEN↓ vstack) по зонах:
   дах+дормери, стрічка вивісок (літери!, апостроф КАВ'ЯРНЯ), шахти сходів, кути будівлі.

## Уроки Хігсфілд (smarts, 07/2026)

- **1k НЕ тримає геометрію** повного фасаду: дормери сплющуються, шахти перемальовуються.
  Тільки `resolution: "4k"` (закон towns-higgsfield-4k-only підтверджено і для NBP i2i).
- NBP на сонці любить робити темний графітовий дах КОРИЧНЕВИМ — прямо писати
  "roof stays DARK GRAPHITE metal, no color shift to brown".
- Апостроф у вивісці КАВ'ЯРНЯ губиться — прямо писати "КАВ'ЯРНЯ keeps its apostrophe".
- Завжди count=2: розкид якості між кандидатами одного промпту великий.

## Що скор НЕ ловить (тому очі обов'язкові)

- Дзеркальні/дрібні зміни ТЕКСТУР (цегла↔штукатурка того ж тону) при збігу контурів;
- зміну КОЛЬОРУ секції (edge-мапа сіра) — колір звіряти по свотчах у парі було/стало;
- галюцинації в зонах, де в REF немає контурів (порожнє небо → нова труба = зловить, але
  новий відтінок неба = ні).

## Для ВІДЕО (kling день→ніч)

- Кадр 0 і фінальний кадр відео проганяти через geo-qa проти відповідних майстрів (М1/М2).
- Проміжний кадр (середина) — проти М1: перспектива не повинна «плисти» в русі.
- Перший кадр keyframe-пари = ЗАВЖДИ фотореальний майстер (закон kling-never-raw-3d-keyframes).
- **Keyframes для hero-фону = БЕЗ ЛЮДЕЙ.** Якщо на кейфреймі стоїть людина, kling робить
  з неї привида (стоїть → зникає). Людей вичищати NBP-едитом ДО відео (smarts, 07/2026).
- **День-кейфрейм під білі титри = м'яке приглушене небо**, не сонячний хай-кей: засвічений
  день з'їдає білий текст hero навіть крізь scrim. Гейт це не ловить — перевіряти контраст очима.
