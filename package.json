{
  "name": "award-re-springs-container",
  "private": true,
  "description": "Container deps for the springs replica pipeline. playwright PINNED to 1.56.0 because the pre-installed chromium in the web/cloud container is build 1194 and `npx playwright install` is egress-blocked (cdn.playwright.dev denied). 1.56.0 == chromium revision 1194 exactly; a newer minor (1.57 -> 1200) will NOT match the pre-installed browser. See KAI/STATE-springs.md § ЯК ПОЧАТИ.",
  "dependencies": {
    "playwright": "1.56.0",
    "playwright-core": "1.56.0"
  }
}
