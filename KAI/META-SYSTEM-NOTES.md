# МЕТА-СИСТЕМА — задум Єгора (зафіксовано 2026-07-06, сесія 5)

> Статус: ЗАДУМ, не будувати до закриття AIR-COMPLETE. Єгор: «коли перейдемо
> до нових задач — база компонентів/прийомів (вона в eruhomist, він додасть
> фолдери сюди після AIR), галерея, суперправила створення сайтів по сенсах/
> копірайту/секціях/композиціях — візьмемо з links/ ноуледж більше і
> побудуємо мета-систему».

## Що вже Є і стане фундаментом (не починати з нуля!)
- **Граматика**: `_REGISTRY_TID.md` (T-ID прийоми, 5 VI-сайтів + AIR 49 рядків) — це вже «мова прийомів».
- **Контракти**: RECIPE.md формат (what/when/lands/not_when + acceptance + живі величини в tokens.json з "law") — готовий формат картки прийому для галереї.
- **Рейки процесу**: BUILD-PROTOCOL (порядок юніта) + ANTI-ISC (гейти-назавжди) + live-archive (законсервовані оригінали) + verify/index пайплайн + вердикт-цикл (пари відео).
- **Копірайт-волокно**: агент copy-fedoriv + brand.md-формат + Fedoriv-реєстр (нуль емдашів, живі речення).
- **eruhomist-база**: компоненти/прийоми з попередніх сайтів (Єгор додасть фолдери ПІСЛЯ AIR — тоді обʼєднати з library/ під одним реєстром).

## Мапа: LifeOS Міслера → наша мета-система (що брати з links/links.md)
| Його система | Наш аналог/застосування |
|---|---|
| **Isa/ISC** (Integrated Success Criteria як жива специфікація) | RECIPE.acceptance → підняти до єдиного формату «критерій → машинна проба → вердикт»; ISC-статуси замість status: candidate/official |
| **Skills__SkillSystem** | атоми/комбо як викликувані скіли з тригерами (коли який прийом брати — зараз це combines_with/when, формалізувати в роутер) |
| **Algorithm__AlgorithmSystem** | наш pipeline (жива правда → атом → проба → вердикт → зборка) як ОДИН генералізований алгоритм для БУДЬ-ЯКОГО референс-сайту (AIR-COMPLETE = перший прогін) |
| **Fabric** (патерни-промпти) | «суперправила по сенсах/копірайту/секціях/композиціях» = набір fabric-патернів: extract-meaning, write-section-copy, compose-page, judge-composition |
| **Hooks__HookSystem** | вже почато (pre-commit, SessionStart, linkcheck) — розширити Stop-хуком (SuccessClaimGate-аналог) і freshness-гейтом |
| **Memory__MemorySystem + CurationCoverage** | ANTI-ISC + STATE + learnings → єдина курована памʼять з покриттям (яка частка прийомів має уроки/вердикти) |
| **Telos** | вже в ужитку (TELOS-файли); мета-система підвішується під цілі студії |
| **Testing__TestingDoctrine** | вже перенесено ядро (гейти, anti-ISC, база warnings); додати property-тести на движки (fast-check-стиль) |
| **Router/Arbol/Work** | оркестрація задач студії (пізніше, коли буде кілька паралельних сайтів) |

## Порядок (як прийде час)
1. Єгор додає eruhomist-фолдери → інвентаризація і злиття під один реєстр/verify.
2. ГАЛЕРЕЯ: одна сторінка-каталог всіх official-прийомів з лабами і відео (є зачаток GALLERY.html).
3. СУПЕРПРАВИЛА: дистилювати з тірдаунів 5+ сайтів fabric-патерни (сенси/копірайт/секції/композиції).
4. МЕТА-АЛГОРИТМ: «новий сайт з нуля» = Algorithm-прогін по фабричних патернах + галереї.
5. Хуки-розширення з ANTI-ISC «кандидатів».
