---
id: floorplan-tower--era
name: "Floorplan TOWER era (ERA dark sister of tower--ever: the same full EVER-grade drill on the same large 2-korpus tower, re-skinned ERA dark copper, with a cinematic photo-bleed flat-detail: facade floor-hover with a y-tracking copper tooltip -> clean copper numbered floorplate of 8 units round a core -> dark veil flat-detail where a warm evening interior bleeds behind a giant serif area numeral; controlled-click, openly a demo project)"
role: floorplan
kind: combo
status: base
family: floorplan-tower
palette: era-dark
section: floorplan
map_style: ""
composition: "TOWER full drill (ERA dark re-skin): korpus toggle + facade floor-hover (y-tracking copper tooltip) -> copper numbered floorplate (8 units round a central core, standard top card) -> dark veil flat-detail with a cinematic photo-bleed (warm interior behind a giant serif area numeral)"
webgl: false
ease: award
entry:
  call: "combo-lab.html - clean-floor-hover-select over the Higgsfield tower render (traced bands) + numbered-floorplate-select over the authored tower-floor-clean.svg + veil-flat-detail-reveal. Synthetic demo data from _shared/floorplan-helpers.js (TOWER_*) + _assets/floorplan tower vectors. The L3 signature reveals fp-assets/interior-evening.webp (decode-guarded)."
  module: section-variant
meaning:
  what: "The ERA dark sister of floorplan-tower--ever: the SAME full visual-search drill on the SAME large multi-korpus tower, re-skinned to ERA cinematic dark copper, plus ONE signature move. L1: the Higgsfield tower render on a navy stage with a korpus A/B toggle and traced floor bands; hovering a floor tints it copper + a dark copper tooltip card y-tracks the hovered floor naming the floor and its free count. Click -> L2: a copper-themed numbered floorplate of 8 units round a central core (the authored tower-floor-clean.svg) with a standard top card + a persistent unit list. Click -> L3: a dark veil flat-detail whose SIGNATURE is a cinematic photo-bleed: a warm evening interior image bleeds behind a giant italic serif AREA numeral on the left, with the dark copper plan panel beside it. Controlled-click; 0 pins; NO-WebGL."
  when: "A large multi-korpus / many-floor tower wanting the full EVER-grade building->floor->unit->flat drill in an evening, cinematic register (dark copper) rather than the light EVER replica. The mood-led member of the family; uses the same openly synthetic demo dataset as tower--ever."
  signature: "The L3 cinematic photo-bleed: a warm evening interior bleeds (cover, dimmed) behind a GIANT italic serif area numeral on the left of the flat-detail, the dark copper plan panel beside it; paired with the ERA dark copper re-skin and the y-tracking copper facade tooltip. The bleed image is decode-guarded so a missing file never throws."
  lands: "A lit tower of two wings stands on near-black; move up it and a copper band warms a floor's window row while a small dark card glides alongside naming the floor and its free count. Click and a copper plate of that floor arrives, eight apartments round the core each marked by its number, a list beside them. Click a number and the screen darkens and lifts to an evening apartment page: a warm room glows behind a tall serif number that is the area itself, the clean plan drawn in copper beside it, the price below. It reads like ERA after dark, on our own tower."
source:
  grammar: "ERA dark register over the EVER visual-search L1-L3 grammar: clean-floor-hover-select (traced perspective bands + a y-tracking tooltip, re-skinned copper) -> numbered-floorplate-select (centroid number pills round a core, copper plate) -> veil-flat-detail-reveal (dark two-tone detail) plus a cinematic photo-bleed numeral. ERA dark tokens (navy stage, copper #c08457 accents)."
  recording: "apps/quadro/.award-re/teardowns/D_era_springs_visualsearch_video.md (ERA dark palette + L3 compositions) + D_ever_visualsearch_video.md (L1-L3 measurements)."
  registry_ref: ["ever-visual-search-drilldown", "TOWER-1to1-replica", "ERA-dark-cinematic-bleed"]
uses:
  - { atom: clean-floor-hover-select, job: "L1 pick a floor on the Higgsfield tower render; perspective band quads traced off a coordinate grid (viewBox 3712x4608, fit contain); the copper left tooltip y-tracks the hovered floor; korpus A/B re-themes the bands" }
  - { atom: numbered-floorplate-select, job: "L2 copper-themed numbered floorplate of the authored tower-floor-clean.svg (viewBox 100x72, 8 unit footprints round a central core); centroid number pills + status hover wash + standard top card" }
  - { atom: veil-flat-detail-reveal, job: "L3 dark cinematic flat-detail via the dark veil; authored clean tower type-plan + spec rail, with a photo-bleed of a warm evening interior behind a giant serif area numeral" }
pin: { owner: none, count: 0 }
pin_killed: []
asset_truth: "OPENLY SYNTHETIC (the same demo tower as tower--ever, no real data), carrying the legal line 'Демонстраційний проєкт. Дані наведено для прикладу.' The data is SELF-CONSISTENT (4 types area x flat rate $1450/m2 so price never contradicts area; 8 units/floor matching the authored 8 footprints; statuses spread by floor band). The tower render is Higgsfield-generated; floor bands are TRACED over the real render off a coordinate grid (not guessed). The L3 evening interior (fp-assets/interior-evening.webp) is a Higgsfield mood image, decode-guarded so a missing file never throws. Floor count capped at the readable bands (B3 gate). NEVER claimed as real."
webgl: false
owns_pin: false
gated_by: [R_pin_budget, R_perf_limits, R_no_webgl]
acceptance:
  - "FULL drill korpus+floor->unit->flat, ERA dark re-skin: L1 traced facade bands + y-tracking copper tooltip + korpus A/B toggle; L2 copper numbered floorplate of 8 units round a core + a unit list + a standard top card; L3 dark veil flat-detail with the cinematic photo-bleed numeral. Verified by screenshot at every level."
  - "SIGNATURE photo-bleed: a warm evening interior bleeds (cover, dimmed) behind a giant italic serif AREA numeral on the left; the dark copper plan panel sits beside it; the image is decode-guarded so a 404 never throws and the rail still renders."
  - "OPENLY SYNTHETIC + self-consistent: carries 'Демонстраційний проєкт. Дані наведено для прикладу.'; prices = area x 1450 (never contradict area); 8 units match the 8 authored footprints; all 3 statuses render. NO claim of being real."
  - "CONTROLLED-CLICK, PIN-LESS: 0 pins, no scroll-stepping, no scale-from-origin. pins === 0."
  - "PLAN IS HERO + SPEC VISIBLE: the unit list (nr/type/area/price/status) is visible beside the plate; the flat-detail rail (type/sub/area/price/reserve) is visible; max 1 full plan at a time."
  - "F-22 canvas full: a big copper plan + a visible floor-locator (THIS unit highlighted) + the photo-bleed numeral fill the canvas; no large empty region."
  - "F-21 prominent back: .vfd-back is a clear copper pill with a circular arrow + hover, not a faint text link."
  - "floor bands TRACED over the real render (read off a coordinate grid, not guessed); floor count capped at the readable bands (B3)."
  - "motion only transform/opacity/clip-path/filter/stroke-dashoffset; NO WebGL/mix-blend/backdrop/canvas/scale-from-origin. decode-guard the render + plans + the bleed image. reduced-motion/<=900px -> floor + unit lists, bleed hidden. __LAB_OK__ true, 0 console errors."
  - "copy Ukrainian; ZERO em-dash/en-dash anywhere; $ prices."
gate:
  probe: "Open combo-lab. __LAB_OK__ true, 0 pins, 0 console errors, render painted. Hover up the tower: a copper band lights a floor + a dark copper tooltip y-tracks it; korpus A/B toggles re-theme the bands. Click a floor -> 8 numbered copper units round the core + a unit list (self-consistent demo data, all 3 statuses, $ prices). Click a unit -> the dark veil opens the cinematic flat-detail: a warm evening interior bleeds behind a giant serif area numeral, the copper plan + spec beside it. Confirm the 'Демонстраційний проєкт' legal line. Eyeball every level."
files: [combo-lab.html, RECIPE.md]
note: |
  TOWER family ERA SISTER of floorplan-tower--ever. Same Higgsfield tower render (tower-front.png, two
  korpus, ~9-10 readable floors per the B3 cap) + same authored tower-floor-clean.svg (8 footprints round
  a core) + same 4 authored clean type-plans + the same self-consistent synthetic dataset (TOWER_* in
  floorplan-helpers). Re-skinned ERA dark copper (navy stage, copper #c08457 bands/pills/accents, dark
  veil). SIGNATURE = the L3 cinematic photo-bleed (warm evening interior fp-assets/interior-evening.webp
  behind a giant italic serif area numeral, decode-guarded). Openly a demo with the legal line. Composes
  all THREE atoms. Sister variants: ever (light replica) + springs (cream sepia + left preview-pane).
---

# floorplan-tower--era - the ERA dark sister of the EVER tower replica

ERA dark. The same full drill on the same Higgsfield tower, re-skinned cinematic dark copper: facade
floor-hover with a y-tracking copper tooltip + korpus A/B toggle -> copper numbered floorplate of 8 units
round a core (standard top card) -> dark veil flat-detail whose SIGNATURE is a cinematic photo-bleed (a
warm evening interior behind a giant italic serif area numeral, the copper plan beside it). Openly a demo
project (self-consistent synthetic data + a legal line); controlled-click; the bleed image is decode-guarded.

## Proven (the lab)
__LAB_OK__ true, 0 pins, 0 console errors, render painted. Atoms ran: clean-floor-hover-select +
numbered-floorplate-select + veil-flat-detail-reveal. Drive: hover the tower -> copper floor band +
a dark copper tooltip ("12 / поверх / 5 вільних · 8 квартир") that y-tracks; korpus A/B toggle; click a
floor -> 8 copper numbered units round the core + a list (01 3 спальні 96.5 м² $139 925, 02 Студія
Продано, 04 Бронь ...) all 3 statuses, $ prices scaled by area; click a unit -> the dark veil opens the
cinematic flat-detail: a warm evening interior bleeds behind a giant serif area numeral ("96,5"), the
copper plan + spec + Забронювати перегляд beside it. The bleed is decode-guarded (a missing
interior-evening.webp leaves only the warm wash + numeral, never throws). Legal line present. Screens
reviewed by eye.
