# SOURCE — scroll-snap
- ОРАКУЛ: served markup springs-home (×20 data-scroll-snap-point; витяги в CD-RUN-snap-morph-v1/NARRATION.md). Плівка закон не показує.
- BUILD: Claude Design, проєкт fa221289 (scroll-snap.html) → RAW-cd-output.html → build.html.
- Гейти S38 (s38-quad-gate.mjs): ALL PASS на RAW, 0 фіксів механіки — таргети 0/900/…/5400 зростаючі; мертва зона без снапу; РЕАЛЬНИЙ wheel → glide точно в станцію (y=1800±0); «нуль контент-твінів» числами (meterFrac=f(scroll) ±0.000); скасування інпутом; render() не арм-ить.
- Фікси S38 (2, видимість): тексти живуть лише на видимій плиті (init autoAlpha 0 на невідкритих; out перша половина wipe / in друга половина). Механіка не змінена, гейти перегнані ALL PASS.
- G22 collision-частина: collisions=0 після фіксів (було: стек словами всіх плит op=1). Video-парність із live не вимірюється (закон з markup, суто dummy-фасад).
