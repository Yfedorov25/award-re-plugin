# ATOM: hero-intro — мозаїчний hero: проява під вуаллю, пізній хедер, wordmark-колапс на виході (v1)

## СУТЬ
Hero springs (l-intro): діагональний мозаїчний грид живих тайлів (~-14°, широкий темний grout) +
display-титул серифом справа-внизу + абзац зліва + scroll-hint. Вхід = цілісна проява під
зеленою вуаллю (хендоф від прелоадера), хедер заходить ІЗ ЗАПІЗНЕННЯМ. Вихід на скролі =
наступна сцена накриває пінований hero (закон wipe-up #3) + ДВА власні закони: (а) wordmark
хедера колапсує право→ліво до самої монограми, що ковзає в центр — чиста f(p); (б) титул
вхідної сцени реве́литься ПОРЯДКОВО з-під baseline-масок у scrub-вікнах прибуття.

## МЕХАНІКИ (FILM 22.41.08, t≈1.9–8s; наратив Єгора ВІДСУТНІЙ — гіпотези позначені)
LOAD-годинник (one-shot, ≈2.8s; renderIntro(p)):
- M1 ВУАЛЬ (i0–0.45): hero під суцільною зеленою вуаллю; вуаль op→0, hero освітлюється під нею.
  ТЕКСТИ HERO ВЖЕ СКОМПОНОВАНІ — без поелементних входів (закон з плівки).
- M2 TRAIL (i0.35–0.75): центральний wordmark вуалі тане повільніше за вуаль (спадок прелоадера).
- M3 ХЕДЕР ПІЗНО (i0.75–1.0): пауза ~1.2s після проявленого hero → хедер одним груповим
  fade + y −12→0 (~0.6s). Більше нічого не рухається.
- M4 ЖИТТЯ ТАЙЛІВ: повільний in-tile дрейф (scale-петлі, десинхронізовані фази) = стенд-ін живого
  відео; поза обома годинниками, дозволено (контент, не хореографія).
SCROLL-годинник (ОДИН pinned trigger, scrub; render(p)):
- M5 COVER (p0–0.55): наступна сцена їде знизу поверх пінованого hero твердим швом 1:1 (закон
  wipe-up); грид під нею живе scale 1.0→1.045 ease:none; тексти hero НЕ рухаються.
- M6 WORDMARK-КОЛАПС (p0.05–0.60, підписний закон): літери wordmark зникають ПРАВО→ЛІВО під
  clip-ом (слово видимо коротшає), монограма ковзає зі свого верхньо-правого сідала в оптичний
  центр хедера. Монотонно зі скролом, повністю реверсивно, чиста f(p).
- M7 ТИТУЛ ВХІДНОЇ СЦЕНИ (p0.55–0.95): порядкова проява 3 рядків серифу з-під baseline-масок
  (низи гліфів обрізані посеред прояви — доведено кадром h6), стаггер-вікна ~.55–.72/.62–.80/
  .70–.88; caps-абзац останнім (~.78–.92); блок дрейфує вгору ≤6vh ease:none (parallax-відчуття).
- M8 SETTLE (p0.95–1.0): все в спокої; вихід далі = інший атом.

## ТЕМП (live)
Hero проявлений ~t2.1s → хедер ~t3.5s (пауза ~1.3s) → перший скрол ~t3.9s; накриття ~0.5s
скролу; титул доведений до повного ~t5.5s (продовжується у флоу зі своїм parallax).

## ДЖЕРЕЛА / СТАН
FILM: `Screen Recording 2026-07-12 at 22.41.08.mov` t≈1.9–8s (кадри в
`~/Downloads/CD-RUN-preloader-hero-v1/reference/beat-h1..h7.jpg`).
NARRATE: ⬜ ВІДСУТНІЙ — гіпотези нижче.
BUILD: через Claude Design — пакет `~/Downloads/CD-RUN-preloader-hero-v1/` (промпт 02).

## ВІДКРИТІ ПИТАННЯ (закрити наративом Єгора або v2)
1. Хедер: чи справді один груповий fade+y (проміжних кадрів мало)? 2. Зум гриду при накритті —
є/нема (будуємо стримано 1.045)? 3. Wordmark-колапс — чи суто скролом (на плівці монотонний зі
скролом, але міг бути threshold-твін)? 4. У live титул вхідної сцени живе у флоу з parallax —
в атомі спрощено до scrub-вікон одного pinned band (звірити оком, чи прийнятно).

## TRANSFER-ГЕЙТ
Той самий закон на іншому фасаді: інша кількість тайлів/кут гриду, інше ім'я (коротше/довше
слово — колапс масштабується), 2-рядковий вхідний титул — пізній хедер, колапс і baseline-маски
працюють.

## СТАН ПІСЛЯ CD (S38)
BUILD = CD (проєкт be28d830), механіка RAW без фіксів; гейти ALL PASS; 1 правка видимості
(гасіння накритих hero-текстів @p.33–.40 — прецедент interiors) → collisions=0.
Вікна білда: LOAD: вуаль 0–.45 · trail .35–.75 · хедер 2.1s (.75–1.0). SCROLL (pin +140%):
cover 0–.55 · wordmark-колапс .05–.60 (clip right 0→103%, монограма в центр) · рядки
.55/.62/.70 (+.17-.18) · sub .78–.92 · settle .95–1.0. ✅ APPROVE Єгора 2026-07-17 («ці приймаю»).
