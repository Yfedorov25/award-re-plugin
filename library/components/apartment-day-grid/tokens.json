{
  "id": "apartment-day-grid",
  "built_on": ["daynight-engine"],
  "model": "A — all rooms at once, synchronized day",
  "defaults": {
    "hours": [7, 23],
    "stagger": 0.05,
    "scrub": { "start": "top top", "end": "+=200%", "pin": true },
    "grid_cols": { "desktop": 3, "tablet": 2, "mobile": 2 }
  },
  "room_shape": "{ day, night, name }",
  "sync": "one scroll t is broadcast to EVERY cell's set(t); optional small stagger reads as a gentle wave but a cell never leads/lags by more than `stagger`",
  "clock": { "from": "07:00", "to": "23:00", "formula": "7 + t*16" },
  "phases": { "day": "<0.34", "dusk": "0.34..0.7", "night": ">=0.7" },
  "perf": "per-night-layer will-change cleared (N stacked cells under a scroll would churn the compositor otherwise)",
  "laws": ["no-webgl", "no-canvas", "no-mix-blend", "frame-matched-pairs-only", "time-equals-lighting-everywhere", "owns-pin", "reduced-motion-static-dusk"]
}
