{
  "$extends": "slice-clip",
  "$variant": "slice-top-down",
  "note": "Knob DIFF over the base slice-clip (frs7, clip wipes LEFT->RIGHT, keeps its vertical bronze edge). The base engine (component.js) is parameterized over `dir` and `edge`, so this variant is PURE DATA (CONTRACT §3 preferred path): flip the clip axis to TOP->DOWN and turn the edge-line OFF. No new DOM layer, no new phase, zero code. Byte-faithful to frs8-slice-td-clip.html.",
  "params": {
    "dir": "td",
    "edge": false
  },
  "engine": {
    "model": "same parity-locked two-<img> clip-wipe as the base; only the inset axis changes",
    "closedInset": "inset(100% 0 0 0)   (incoming layer fully hidden ABOVE)",
    "openInset": "inset(TOP% 0 0 0)   TOP 100% (hidden) -> 0% (full) — the new photo WIPES DOWN over the old",
    "edge": "REMOVED on purpose — the bronze edge-line travelled OPPOSITE the actual clip seam (a stray horizontal line from the wrong side), so the top-down variant ships edge:false. Do NOT re-add it.",
    "shared": "frs3 composition (rail 38% / render 62%, ~2cm gap, bronze), Lenis lerp 0.1 smoother, pure render(prog), pin/pinSpacing/scrub:true, dwell 0.86 / hold 0.78 / pinFactor 1.4, glide wipe ease + air rail ease, transform/opacity/clip-path only, reduced-motion/<=820px static stack"
  }
}
