{
  "id": "oval-mask",
  "engine": "EVER portrait OVAL with media inside (the hero 'eveR' oval = a looping video between the letters; ARCHITECTURE/INTERIOR ovals). The mask shape is STATIC; the media inside parallaxes. Harvested from the live read.",
  "measured_live_EVER": {
    "shape": "clip-path: ellipse(...) — a portrait oval ~0.62-0.69 ratio (measured 121x175 ~= 0.69; hero ~0.62).",
    "inner": "media (a looping muted video in the hero; a render elsewhere) over-scaled ~1.08 and drifting a few px on pointer/scroll — parallax-WITHIN. The MASK never deforms (no morph); only the content moves.",
    "use": "EVER's right hero half: a dark panel with this oval between the wordmark letters, a looping clip inside."
  },
  "knobs": {
    "ratio": { "default": 0.66, "what": "portrait oval w:h (the wrap's aspect-ratio gives the shape; the clip is ellipse(50% 50% ...))." },
    "scale": { "default": 1.1, "what": "inner media over-scale so the parallax drift never reveals an edge." },
    "parallax": { "default": 10, "unit": "px", "what": "max drift of the inner media (pointer + scroll)." },
    "drive": { "default": "pointer", "options": ["pointer","scroll","both","none"], "what": "what drives the parallax-within." }
  },
  "entry": "OvalMask.init(target, opts) -> { masks, destroy }. Markup: <div class='ovm' data-oval-mask><video|img class='ovm__media'>…</div> (the wrap's aspect-ratio sets the oval; the engine sets the ellipse clip + drives the inner media; autoplays a <video> inside).",
  "law": "clip-path (STATIC shape, never animated/morphed) + transform (inner drift/scale) only; GPU layer on the inner media; NO mix-blend / NO backdrop; reduced-motion / drive:none -> static (media centred). NO WebGL.",
  "gate_mode": "eye-check + parallax-stability (NOT the blank/solid-<img> check): a single decorative oval on a panel is mostly panel + a <video> inside — the gate's solid-media check looks for a full-bleed <img> and reads the panel as blank (a false fail). The real gate: the clip is IDENTICAL as the pointer moves (mask static) AND the inner media tx differs (parallax-within), confirmed in a real browser; fps>=50, no jank.",
  "proven": "lab: OUR clip-river video in a 0.66 portrait oval on a dark EVER-panel. Clip stable (ellipse identical left vs right pointer = mask never deforms); media drifts tx -11 (left) -> +11 (right) = parallax-within; video readyState 4 (playing); 59.9fps. window.__LAB_OK__ set."
}
