{
  "id": "numeral-frame-expand-hero",
  "engine": "r1864's SIGNATURE hero — one continuous pinned scroll-narrative in 4 stages: (A) FRAME-EXPAND: giant outline NUMERALS ('1864') on warm-black with a small framed view (Kremlin-through-columns) sitting INSIDE the numeral row; on scroll the frame EXPANDS to a full-bleed photo (clip-inset -> 0 + scale -> 1 + inner img fades up) while the outline numerals fade out. (B) TITLE-IN: the serif display title (УРОВЕНЬ ЖИЗНИ / КЛАССА DE LUXE / БЕЗ КОМПРОМИССОВ) + sub-line fade + rise in. (C) PARALLAX-PAN: a slow vertical drift of the background while the title holds. (D) EXIT-RISE: hero opacity -> 0 + translateY rise as the next (light) section arrives. Harvested from D_r1864 (r18642 + r18641 x_001..x_020). Absorbs 4 reported hero fragments into one engine.",
  "measured_teardown": {
    "move": "one pinned scroll-scrub, progress p 0..1. (A) 0.00..0.35: .nfe-frame clip-path inset frameInset%(34)->0 + scale frameScale(0.62)->1; inner img opacity 0.4->1; .nfe-numerals opacity 1->0 (gone by ~0.30). (B) 0.30..0.55: .nfe-title opacity 0->1 + translateY 30px->0; .nfe-sub a beat later (0.42..0.66). (C) 0.55..0.90: .nfe-bg translateY 0 -> -panY(120)px. (D) 0.85..1.00: .nfe-hero opacity 1->0 + translateY 0 -> -exitY(160)px. ease power2.out per channel.",
    "cited": "r18642: outline '1864' on warm-black, a framed Kremlin-through-columns view inside the numerals (x_004) -> the frame expands to a full-bleed embankment photo (x_014) -> serif title 'УРОВЕНЬ ЖИЗНИ / КЛАССА DE LUXE / БЕЗ КОМПРОМИССОВ' over the photo (x_022) -> vertical parallax pan (x_030)."
  },
  "knobs": {
    "frameInset": { "default": 34, "unit": "%", "what": "how clipped the centre frame starts (clip-path inset); collapses to 0 as it expands to full-bleed." },
    "frameScale": { "default": 0.62, "what": "the centre frame's starting scale; grows to 1 over the expand." },
    "panY": { "default": 120, "unit": "px", "what": "vertical parallax drift of the background during stage C." },
    "exitY": { "default": 160, "unit": "px", "what": "translateY rise of the whole hero during the exit (stage D), as it fades to 0." },
    "pinFactor": { "default": 2.4, "unit": "x innerHeight", "what": "length of the pinned scroll span — the whole 4-stage narrative plays across it." },
    "ease": { "default": "power2.out", "what": "per-channel scrub curve (expo.out available)." },
    "manageLenis": { "default": true, "what": "own a Lenis instance if present; false when the page drives a shared Lenis (lab sets false)." }
  },
  "entry": "NumeralFrameExpandHero.create(target, opts) -> { trigger, lenis, set(p), refresh(), destroy }. target = .nfe-hero > .nfe-bg(.nfe-frame > img) + .nfe-numerals(.digits) + .nfe-title + .nfe-sub. set(p 0..1) is a PURE scrub of the 4-stage narrative.",
  "law": "clip-path inset + transform(scale on frame, translateY on bg/hero) + opacity only; GPU; NO mix-blend over the scrubbed photo; NO WebGL; NEVER scrub video.currentTime (use an <img>, or a poster); reduced-motion or <=820px -> final state (photo + title shown, no exit). owns_pin TRUE. GSAP + ScrollTrigger required; set(p) standalone.",
  "gate_mode": "owns_pin scroll-scrub. Verify the 4 stages in order: (A) frame inset 34->0 + scale 0.62->1 + numerals fade; (B) title op 0->1 after; (C) bg pan; (D) hero exit op->0 + rise. GATE-BLINDSPOT: clip-path media -> a naive blank% probe false-blanks early; verify the inset/scale/opacity curve + fps. Drive the lab's own pinned instance to screenshot (do NOT create a 2nd instance).",
  "proven": "lab: OUR QUADRO aerial render in the frame. Curve measured live (p / frame-inset / frame-scale / numerals-op / title-op / bg-pan): 0 / 34% / 0.62 / 1.00 / 0.00 / 0 -> 0.18 / 8% / 0.91 / 0.16 / 0 / 0 -> 0.35 / 0% / 1 / 0 / 0.36 / 0 -> 0.55 / 0% / 1 / 0 / 1.00 / 0 -> 0.75 / 0% / 1 / 0 / 1 / -98px -> 1 / 0% / 1 / 0 / 1 / -120px (+ exit op->0, rise -160px over 0.85..1). Screenshots: (1) outline '1864' + the framed QUADRO aerial inside the numerals; (2) frame expanded full-bleed + serif 'УРОВЕНЬ ЖИЗНИ / КЛАССА DE LUXE / БЕЗ КОМПРОМИССОВ' + sub-line (matches r1864). Probe (4x CPU throttle): 1/663 long frames (0.2%), 59.9fps -> PASS. Zero console errors. Serif stand-in = Playfair Display (Didot/Bodoni class)."
}
