{
  "id": "budget-pills-bridge",
  "engine": "AIR /investment B4 bridge (contract D_AIR_invest_1to1 B4, live-confirmed A-12/T-409): after EACH strategy model, a row of 2-3 budget-range buttons deep-links into the catalog with a price pre-filter `?price[from]=F&price[to]=T` (the owner's recording shows the exact query in the AIR status bar). The section does not narrate - it sells a concrete slice of inventory. SHAPE: pills (coordinator decision 2026-07-05 + constitution C12: no naked rectangular ghost buttons; the contract measured small-radius RECTANGLES - the deviation is documented in the RECIPE for the owner's verdict) at contract dimensions: ~4.4vh height, plus icon on the right, >=44px tap targets on mobile. The range numbers are FILTER BOUNDS (interface), anchored to canon inventory prices (22 800 smart, 67 000 townhouse), never market claims (F1).",
  "measured_teardown": {
    "move": "markup-first: <nav data-bpb data-bpb-base> holds <a data-bpb-pill data-from data-to> anchors; the component only assembles hrefs (base + 'price[from]=F&price[to]=T', literal bracket syntax exactly as AIR emits it) and injects the plus glyph. All states are CSS; zero animation engines involved.",
    "cited": "AIR v4 t~9.6 + repeat after Asset Resale: UP TO 50 MILLION / 50-150 MILLION / 150+ MILLION, light-gray, plus icon right, hover reveals the deep-link in the status bar: aircenter.space/offices?price[from]=...&price[to]=..."
  },
  "knobs": {
    "base": { "default": "#каталог", "what": "deep-link base. STUB until the visual-search catalog exists; the probe verifies the parameter FORMAT, not the receiver. Swap to /offices when the catalog lands - the query contract stays identical (A-12/T-409)." }
  },
  "css_surface": {
    "--bpb-h": "pill min-height (default 4.4vh per contract; mobile forces >=44px)",
    "--bpb-bg": "pill fill (default rgba(17,17,16,.07))",
    "--bpb-bg-hover": "hover fill (default rgba(17,17,16,.13); AIR showed no distinct hover state - kept minimal)",
    "--bpb-ink": "pill text (default #111110)",
    "--bpb-font": "pill font-size (default 12px CAPS)"
  },
  "entry": "BudgetPillsBridge.create(target, opts) -> { root, pills, links(), destroy, ready }. target = the [data-bpb] nav. links() returns the assembled hrefs for machine probes.",
  "law": "zero motion beyond a CSS hover tint (no scroll-driven props, D3 untouched); hrefs carry the literal `price[from]=`/`price[to]=` contract; range numbers are UI filter bounds tied to canon anchors, never invented market facts (F1/F7); C12: pills, never naked rectangles (contract deviation documented); the component NEVER touches __LAB_OK__ (B15).",
  "gate_mode": "PROBE_MODE only (navigator.webdriver || ?probe). Lab: loadStability (CLS < 0.1 + anchor drift <= 4px, D14 head-inline); EXACTLY 3 pills whose hrefs contain price[from]= and price[to]= with the markup's own numbers on the stub base; pill shape (border-radius >= height/2 - 1), height ~4.4vh desktop / >=44px mobile, 3 plus icons, single row on desktop (tops +-2px); WCAG contrast >= 4.5:1 against the blended pill fill; page free of x-overflow, text rects >= 16px; console errors == 0.",
  "proven": "lab.html v1: the bridge under the canon CTA line («Обрати місце в ряду», ET-25/29 verb+object formula) on the #ececea panel. Ranges 0-30000 / 30000-70000 / 70000-200000 around canon anchors 22 800 and 67 000. Numbers mirror window.__PROBE of the shipped run at 1440x900 / 390x844 / 1280x720; see RECIPE 'Доведено'."
}
