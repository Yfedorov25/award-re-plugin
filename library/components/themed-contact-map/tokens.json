{
  "id": "themed-contact-map",
  "engine": "r1864's /contacts map — the river-tinted-poi-map DIALECT stripped to a SINGLE destination: the warm-taupe drawn district map (river + parks + roads + a few orienting landmark glyphs) carries ONE pin — the 'ОФИС ПРОДАЖ' (sales office) — that DROPS + PULSES on enter, the '1864' disc marks the residence, beside a CONTACT PANEL (address / phone / hours / a framed CTA). The 'where to find us' map; the third map dialect's CONTACT variant. Harvested from D_r1864 (r18645 /location map dialect; r1864 /contacts single ОФИС ПРОДАЖ pill).",
  "measured_teardown": {
    "move": "scroll-into-view scrub, p 0..1, PURE set(p). The map svg fades 0->1 + a tiny scale 1.03->1 over 0..0.5; the '1864' disc (residence marker) fades + scale 0.6->1 over 0.30..0.6; the single ОФИС-ПРОДАЖ pin DROPS (translateY -22->0 from its bottom anchor) + fades over 0.45..0.75; the contact panel lines + CTA fade + translateY 24->0, staggered, over 0.55..1.0. Once revealed, the pin's RING runs a looping PULSE (scale 1->1.8 + opacity 0.5->0, repeat). ease power3.out.",
    "cited": "r1864 /contacts: the same drawn district map as /location but focused on the sales office — a single ОФИС ПРОДАЖ marker + the address / phone / hours and a 'записаться на показ' CTA. The full POI atlas (many pins) is the /location view; this is the contact view."
  },
  "knobs": {
    "start": { "default": "top 76%", "what": "ScrollTrigger start for the reveal." },
    "pulse": { "default": true, "what": "loop a pulsing ring on the sales-office pin once revealed." },
    "pulseDur": { "default": 2.0, "unit": "s", "what": "pulse cycle duration." },
    "duration": { "default": 1.1, "unit": "s", "what": "reveal duration." },
    "ease": { "default": "power3.out", "what": "per-channel curve (expo.out available)." },
    "once": { "default": true, "what": "play once or reset on leave-back." },
    "pin": { "default": false, "what": "pin the stage while it reveals." },
    "manageLenis": { "default": true, "what": "own a Lenis instance if present; false when a shared page Lenis drives scroll." }
  },
  "entry": "ThemedContactMap.create(target, opts) -> { trigger, lenis, set(p), play(), refresh(), destroy }. target = .tcm-stage > .tcm-map ( svg + .tcm-disc[data-disc] + .tcm-pin[data-pin]( .tcm-pin-ring + .tcm-pin-dot + .tcm-pin-lbl ) ) + .tcm-panel ( .tcm-panel-h + .tcm-line x N + .tcm-cta ). Pin/disc positioned by css vars --tcm-pin-x/y, --tcm-disc-x/y. set(p 0..1) is a PURE scrub of the reveal; the pulse loops post-reveal.",
  "law": "transform + opacity only; GPU; NO mix-blend; NO WebGL. The map is the river-tinted-poi-map drawn SVG (the lab embeds it) so it is asset-independent + demonstrates the dialect reuse. reduced-motion or <=820px -> shown (no pulse, grid stacks to 1 col). owns_pin OPTIONAL (default false). GSAP+ScrollTrigger required for the scroll reveal; set(p) standalone. The pulse is a single GSAP repeat tween (cheap).",
  "gate_mode": "scroll-into-view reveal. Verify: the map fades in, the '1864' disc scales in (residence), the single ОФИС-ПРОДАЖ pin drops + then pulses, the contact panel lines + CTA stagger in. Smoothness via the scroll path (incl. the running pulse). NOTE: a split layout (map left, contact right) — scroll so the stage fills the frame to screenshot the single pin + disc + panel.",
  "proven": "lab: the drawn district map (roads/parks/landmark glyphs Кремль/Храм Христа Спасителя/ГЭС-2/Третьяковская + street-name italics) with the copper '1864' disc + a single 'ОФИС ПРОДАЖ' pin (terracotta dot + pulsing ring), beside a contact panel (АДРЕС Софийская набережная 36 / ОФИС ПРОДАЖ +7 495 280-70-70 / ЧАСЫ РАБОТЫ / 'ЗАПИСАТЬСЯ НА ПОКАЗ' CTA). REVEAL curve measured live (p / map / disc / pin / cta): 0 / 0 / 0 / 0 / 0 -> 0.4 / 0.99 / 0.70 / 0 / 0 -> 0.6 / 1 / 1 / 0.88 / 0 -> 0.8 / 1 / 1 / 1 / 0.59 -> 1 / 1 / 1 / 1 / 1. PULSE: ring opacity animating (0.313 -> 0.121 over 600ms) = the sales-office pin pulses. Zero console errors. Smoothness probe (4x CPU throttle, scroll-enter + pulse running): 306 frames, 0.00% long frames -> PASS. Map = the embedded river-tinted-poi-map SVG (Brick 3 dialect). Relatives: river-tinted-poi-map (the FULL POI atlas: many pins + disc), map-dim-carousel-announce (the map DIMMED as a card-rail backdrop) — this is the CONTACT variant: one focused pin + a contact block, map at full presence. Serif = Playfair Display."
}
