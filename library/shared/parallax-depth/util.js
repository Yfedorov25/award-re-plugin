/* parallax-depth — shared-util STUB. Real code extracted via /award-re:learn --promote.
   Real call: ParallaxDepth.mount(el, opts) */
(function (global) {
  "use strict";
  function mount(/* el, opts */) { return { destroy: function(){} }; }
  global.ParallaxDepth = { mount: mount, __stub: true };
})(typeof window !== "undefined" ? window : globalThis);
