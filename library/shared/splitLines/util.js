/* splitLines — shared-util STUB. Real code extracted via /award-re:learn --promote.
   Real call: SplitLines.mount(el, opts) */
(function (global) {
  "use strict";
  function mount(/* el, opts */) { return { destroy: function(){} }; }
  global.SplitLines = { mount: mount, __stub: true };
})(typeof window !== "undefined" ? window : globalThis);
