/* appear — shared-util STUB. Real code extracted via /award-re:learn --promote.
   Real call: Appear.mount(el, opts) */
(function (global) {
  "use strict";
  function mount(/* el, opts */) { return { destroy: function(){} }; }
  global.Appear = { mount: mount, __stub: true };
})(typeof window !== "undefined" ? window : globalThis);
