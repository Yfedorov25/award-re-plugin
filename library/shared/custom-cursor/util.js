/* custom-cursor — shared-util STUB. Real code extracted via /award-re:learn --promote.
   Real call: CustomCursor.mount(root, opts) */
(function (global) {
  "use strict";
  function mount(/* el, opts */) { return { destroy: function(){} }; }
  global.CustomCursor = { mount: mount, __stub: true };
})(typeof window !== "undefined" ? window : globalThis);
