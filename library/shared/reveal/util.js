/* reveal — shared-util STUB. Real code extracted via /award-re:learn --promote.
   Real call: Reveal.mount(scope, opts) */
(function (global) {
  "use strict";
  function mount(/* el, opts */) { return { destroy: function(){} }; }
  global.Reveal = { mount: mount, __stub: true };
})(typeof window !== "undefined" ? window : globalThis);
