{
  "description": "award-re plugin hooks: (1) inject plugin root + project state on each prompt; (2) design-grounding gate that blocks writing site UI files until art-direction intent exists (the fix for the Phoenix correct-but-cheap failure).",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/start-state.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/design-grounding.js",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
