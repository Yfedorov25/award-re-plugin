# OS
.DS_Store

# plugin runtime artifacts (created in the USER's project, not here — but ignore if present)
.award-re/
prototypes/

# node (if any tooling added later)
node_modules/

# scratch
*.log
library/components/*/variants/*/renders
library/components/*/renders

# visual-parity / site-scan GENERATED output (screenshots, diffs, boards, reports).
# The TOOLS (visual-parity.mjs, site-scan.mjs, ink-box.mjs) are versioned; their
# per-run output is regenerated on demand and must not bloat the repo (scan alone ~96M).
scripts/parity/
scripts/scan/
scripts/visual-parity-report.json
