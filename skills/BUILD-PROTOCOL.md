# BUILD-PROTOCOL — рейки побудови юніта (для БУДЬ-ЯКОЇ моделі)

> Дистильовано 2026-07-06 з сесій 3–5 (Fable 5): 11 атомів, зборка головної,
> 3 фікс-кола, 7 виправлень інтерпретацій. Це НЕ поради — це ПОРЯДОК.
> Слабшій моделі: виконуй кроки ДОСЛІВНО, не пропускай жоден, не імпровізуй
> там, де протокол каже «цитуй». Сильнішій: те саме.

## 0. Перед стартом будь-якої сесії
1. Прочитай `~/Downloads/KAI/STATE.md` → `AIR-COMPLETE-PLAN.md` (черга юнітів).
2. `git status` у репо — дерево має бути чистим; tpreview-* НЕ ЧІПАТИ.
3. Сервер прототипів: `python3 -m http.server 8820 --bind 0.0.0.0 --directory ~/Downloads/award-re-plugin/library`; IP: `ipconfig getifaddr en0` (плаває!).

## 1. ЖИВА ПРАВДА (найважливіший крок — тут народжуються всі помилки)
1. Спершу `skills/teardowns/live-archive/README.md` — можливо, витяг УЖЕ є.
2. Нема — grep у `live-archive/air/*` (DOM-зріз сторінки → клас → CSS → JS).
   Мініфікований shared.js: шукай за іменем патерна/анімації (напр. `imageClipInVertical`),
   друкуй ±800 символів контексту, ЦИТУЙ величини (duration/ease/keyframes) у tokens.json.
3. Кожну знайдену величину ЗАПИШИ в live-archive/README (розділ «ГОТОВІ ВИТЯГИ») — наступному.
4. ЗАКОН ПРІОРИТЕТУ: живий код > відео-тірдаун > реєстрова проза. Розбіжність =
   «виправлення інтерпретації №N» — фіксуй у реєстрі приміткою ⚠️ і в STATE; канон вирішує вердикт Єгора.
5. ЗАБОРОНЕНО будувати механіку «як логічно» — тільки «як у коді/кадрах». Якщо жоден
   з trьох джерел не відповідає — це діра знань → у список дозйомки, НЕ вигадувати.

## 2. АТОМ (конвенції — копіюй сусідній атом, не вигадуй структуру)
1. `library/components/<id>/{component.js, component.css, lab.html, tokens.json, RECIPE.md}`.
2. component.js: vanilla IIFE, шапка-коментар = звідки знято (живі величини!), opts з
   дефолтами-цитатами, `gate` з телеметрією, `destroy()` повний. Скраб = transform у rAF
   БЕЗ transition; свапи = WAAPI подіями; rAF-цикли ГЕЙТОВАНІ (IO/skip-unchanged).
3. status: candidate. official ставить ТІЛЬКИ Єгор.
4. tokens.json: КОЖЕН параметр з полем "law" — цитата живої величини і звідки.
5. Сім'я законів: enableTouch:false (нуль скраб-дрейфів на тачі) · reveal без y-зсуву ·
   спліт-спани display:inline (НЕ inline-block — міняє переноси) · гістерезис порогових
   свапів ~1.5% спану · один пін на біт · no-WebGL (переклад: perspective/clip/filter).

## 3. ЛАБА (кожна — з машинним гейтом)
1. PM-режим: `navigator.webdriver || ?probe`; живий перегляд = НУЛЬ автоскролів.
2. Обов'язкові гейти: loadStability (CLS<0.1 + дрейф якоря ≤4px) · docHeight стабільна
   за прохід · console errors == 0 · КІЛЬКІСНІ пороги на кожну механіку (не «працює», а число ±допуск).
3. Якщо база/старий атом ставить __LAB_OK__ на init — у PM СКИНЬ прапор і став свій вердикт після гейтів.
4. Проба-скрипт: playwright з `createRequire('/Users/yehorfedorov/Downloads/eruhomist/apps/smarts/package.json')`
   (headless працює; MCP-браузер може бути залочений паралельною сесією — не вбивати).
   Чекати `__LAB_REPORT.errors !== null`, НЕ `__LAB_OK__ !== undefined`.
5. Обидва в'юпорти: 1440×900 і 390×844 (hasTouch).

## 4. ВЕРДИКТ-ПАКЕТ (закон: пара відео в ~/Downloads)
1. `AIR-REF--<atom>.mp4` — виріз оригіналу (ffmpeg -ss/-t з mobile-videos/, таймкоди
   шукати контакт-листами fps 0.5–2, дивитись Read-ом).
2. `ATOM--<atom>.mp4` — headless-запис лаби (recordVideo; wheel-події якщо є Lenis,
   webdriver-прапор глушити initScript-ом). Скейл обох у 1600×900 pad + concat.

## 5. ОБЛІК (після КОЖНОГО юніта, не в кінці)
1. `node scripts/library-index.mjs` → `node scripts/library-verify.mjs --no-headless` —
   GREEN, warnings = база (зараз 239; H17: registry_ref = T-### або "x:<чому>").
2. s2/sN.html лінк активувати. Реєстр дожати рядком якщо юніт без T-ID.
3. Коміт одразу (шаблон: `спринт-N: атом <id> (T-XXX) — суть; candidate` + тіло з
   живими правдами і числами проб). STATE.md — 3–6 рядків хендофу.

## 6. ЗБОРКА / ФІКС-КОЛА
1. Сторінкова зборка = `combos/<page>/combo-lab.html`, kind: page-assembly
   (пін-бюджет ПО АКТАХ, pin.count = owns_pin у стеку). Живі фото/копі hotlink.
2. Кожне зауваження Єгора → (а) покадровий розбір його запису (fps 2–3 + контакт-листи),
   (б) корінь ДОВЕДЕНИЙ кодом/кадрами, (в) фікс, (г) НАЗАВЖДИ-гейт у пробу, (д) коміт «фікс-кол N».
3. Розбіжність зборки з оригіналом б'є атом назад у доробку — це нормально.

## Червоні прапори (зупинись і перечитай крок 1)
- Пишеш анімацію, не процитувавши живу величину → СТОП.
- Реєстр/тірдаун суперечить коду → код правий, фіксуй виправлення.
- Проба «майже пройшла» і хочеться розслабити поріг → шукай корінь, поріг не чіпай
  (виняток: точка виміру на повільній кривій — фіксуй чому).
- verify впав або warnings виросли → фікс ДО коміту.
- Хочеться «покращити» механіку понад живу → НІ. 1-в-1, красивості після AIR-COMPLETE.
