# AWARD-RE v1.0 — ПЛАН ПОВНОЇ ПЕРЕБУДОВИ
> Мета: наступний проєкт виходить рівня agronomichne З ПЕРШОГО ПРОХОДУ, без
> «розкачки» з мільйона правок. Джерела: gap-аналіз v0.2 + дистиляція 28 провалів
> (F-01..F-28), motion-score трьох сайтів, 19 memory-уроків.

## 0. ДІАГНОЗ — чому v0.2 видавав джуніор (формула)
Плагін = бібліотека знань із правильним порядком фаз, але:
1. **Одноприйомна модель секції** («реалізуй ПРИЗНАЧЕНИЙ прийом») проти реальності
   Ever: 5-7 прийомів НАШАРОВАНИХ в одному кадрі. Граматики комбінування нема як класу.
2. **Триходова індирекція без примусу**: command → skill-стаб (15 рядків) → reference-есе.
   З ~12 критичних кроків хард-гейт має РІВНО ОДИН (design-intent hook). Решта —
   «читається», тобто ігнорується.
3. **Пайплайн закінчується "Document", не "прод-чек"**: нуль DOM-фактів, нуль деплою,
   нуль прод-проходу сторінок у флоу.
4. **Прототипи де-факто опційні**: «описати рух» легально заміняє показ; портів нема.
   А це ЄДИНИЙ крок, що реально виробляв сеніор-рішення.
5. **Нема петлі навчання**: провали потрапляють у плагін раз на версію через ручний
   постмортем. Відео-тірдаунів не існує.
6. **Агенти отримують посадові інструкції, не контракти**: без DoD, без банів з бойових
   багів, без схеми звіту з доказами.
7. **Копі-знання не там, де пишеться копі**: ДНК хедлайнів («70% прості, гра слів ≤1»)
   лежить в аудит-прикладі; факт-бани (вигадані цитати F-02) — ніде.
8. **Медіа-генерація — сліпа пляма**: плагін чекає «ассети клієнта», реальний флоу
   генеративний (Higgsfield 4K, i2i locks, frame-QA).
9. **motion-score = одноразовий план**, а не жива факт-партитура з вердиктами і дрейф-аудитом.
10. **Аудит одним пострілом наприкінці** без пер-секційного acceptance, ролбек-точок,
    беклогу клієнта.

## 1. АРХІТЕКТУРА v1 — шість стовпів

### Стовп І. КОНСТИТУЦІЯ v1 (закони замість есе)
Файл `CLAUDE.md` повністю замінюється дистилятом 9 розділів (A-I, ~120 законів,
кожен = імператив + ліміт + першоджерело F-номер). Чернетка вже готова:
`CONSTITUTION-v1-draft.md` (у цьому репо). Розділи:
A Пайплайн секції · B Верифікація · C Композиція/Драматургія · D Рух/Перф ·
E Копі · F Чесність даних і медіа · G Ассети (Higgsfield) · H Процес · I Відео юзера.
Правило підтримки: 2+ повтори провалу в FAILURES-LOG = новий закон конституції.

### Стовп ІІ. ГРАМАТИКА КОМБІНУВАННЯ (замість меню прийомів)
Новий core-файл `skills/grammar/_GRAMMAR.md` + реструктуризація реєстру:
- `_TECHNIQUE_REGISTRY` → ТАБЛИЦЯ з ID (T-001…): прийом · роль · ШАР
  (base-media / overlay / text-choreo / micro / seam-transition) · з чим
  комбінується · вартість · сайт-джерело · no-WebGL переклад.
- **Формула секції** (закон C1): СЕКЦІЯ = base-媒дія-шар + 2-3 overlay-шари +
  text-choreo + 1 micro + seam-in/seam-out. Мінімум 5 шарів для flagship,
  3 для службових. section-builder отримує СТЕК ШАРІВ, не «прийом».
- **Партитура дистанцій** (Лінза 5 як процедура): перед білдом — таблиця
  «секція → дистанція камери → що бачимо вперше/вдруге → що перетікає далі».
  Обʼєкт повністю ≤2 рази. Один прийом = одне місце.
- **Шви як першокласні обʼєкти**: реєстр seam-патернів (light-шов, sticky-under,
  нитка, act-слово, theme-flip) із правилом «спосіб переходу не повторюється двічі».
- Wiki-лінки `[[…]]` → реальні відносні шляхи (скрипт-міграція).

### Стовп ІІІ. ПАЙПЛАЙН-СТЕЙТ-МАШИНА З ГЕЙТАМИ (примус замість «прочитай»)
Команда `/award-re:section` стає стейт-машиною. Стан кожної секції —
`.award-re/state/section-XX.yaml` зі стадіями і артефактами-доказами:
```
data-read → design-intent → copy-variants(чат) → prototypes(порти) →
user-choice → integrate → verify-dom → rollback-point → deploy → prod-check → log
```
Кожна стадія закривається ЛИШЕ артефактом:
- copy-variants: повні секвенції В ЧАТІ (AskUserQuestion для текстів — заборонений хуком);
- prototypes: існує /tmp/proto-<sec>/*.html + живий порт + ≥2 варіанти; «описати рух» більше не існує;
- user-choice: рядок у motion-score.md з датою і відхиленими варіантами;
- verify-dom: `scripts/verify.mjs` (їде з плагіном) видає verify-report.json:
  computed-геометрія текстів, overlap, обидва шляхи даних ОКРЕМО (event + storage з
  повним перезавантаженням), стани анімацій, рендер контенту на кожному маршруті
  (`[data-page] section`, не HTTP-200), нуль console.error;
- rollback-point: git-тег + Vercel rollback-URL у ROLLBACK-*.md ПЕРЕД хвилею;
- prod-check: прохід УСІХ пунктів меню на проді DOM-фактами.
Хуки v1 (розмножити design-grounding):
- `gate-design-intent` (є, посилити: пер-секційно, перевірка змісту не розміру);
- `gate-prototype` (deny Write у src секції, поки нема user-choice у state);
- `gate-deploy` (deny deploy-команд, поки verify-report.json старіший за останній build);
- `ban-askuser-copy` (deny AskUserQuestion, якщо у питанні текстові варіанти).

### Стовп IV. АГЕНТИ v2 — контракти з DoD
Кожен агент: вхідний контракт (що ЗОБОВ'ЯЗАНИЙ прочитати) + бан-лист із законів +
DoD-чекліст + схема звіту З ДОКАЗАМИ (file:line / DOM-факт), інакше звіт відхиляється.
- `section-builder`: будує СТЕК ШАРІВ за motion-score; самоперевірка перед звітом:
  пін-бюджет, mobile-no-pin, split-по-словах, нуль порожніх catch, transform/opacity-only,
  один смузер; вихід = код + verify-report.
- `eval-brain` / `perf-guard`: лишаються (вони працювали), додаються нові лічильні
  критерії: шарність flagship-секцій ≥5, дистанційна партитура, theme-ритм.
- `copy-fedoriv`: факт-контракт (F-02/03/04), читає 10 копі-правил перед першим рядком,
  ДНК хедлайнів (70% прості, гра слів ≤1 і тільки hero), бани-файл.
- НОВИЙ `media-director`: веде media-plan.md, генерує по доктрині G (4K, i2i locks,
  frame-QA, кропи з мастерів як макро, пережим із бюджетами), кожна генерація на
  рев'ю парою РЕЗУЛЬТАТ+РЕФЕРЕНС.
- НОВИЙ `teardown-anatomist`: розбирає відеозаписи (юзерські .mov) на граматику.

### Стовп V. ПЕТЛЯ НАВЧАННЯ (плагін, що вчиться)
Нова команда `/award-re:learn` з трьома режимами:
1. **failure** — додає F-запис у FAILURES-LOG проєкту ТОГО Ж ДНЯ (формат: що → чому →
   правило) і, при 2+ повторах, пропонує патч конституції/граматики плагіна.
2. **video-bug** — юзерський запис екрану: ffmpeg fps-кадри → контактні листи →
   діагноз з привʼязкою час/секція → фікс-план. (Канонічний QA-вхід; скрін-серії заборонені.)
3. **video-teardown** — запис чужого сайту: покадрове знімання ГРАМАТИКИ
   (геометрія секцій: позиції/розміри/перекриття/глибини; стеки шарів; тайминги;
   шви; навігаційні патерни; копі-регістр) → генерує D_<site>.md + дельти
   у _GRAMMAR/_REGISTRY (нові T-ID) + чек «чи суперечить конституції».
Кожен build читає FAILURES-LOG проєкту ПЕРЕД стартом (закон A11/H1) — це хук, не порада.

### Стовп VI. САМОДОСТАТНІСТЬ
- `CLAUDE.md` v1 декларує повний tool-stack плагіна: Playwright MCP (DOM-факти,
  прод-чеки), Higgsfield MCP (генерації за доктриною G), ffmpeg (кадри/пережим),
  sharp (композиція), vercel CLI (deploy+instant rollback). Стартовий хук перевіряє
  доступність і чесно каже, чого бракує.
- Заборона залежати від інших плагінів/скілів у RE-флоу: все потрібне (verify.mjs,
  frame-extract.sh, compress-budget.mjs, gen-pages шаблон, prerender-шаблон,
  Telegram-lead шаблон, OSM-проєкція gen.py) їде в `scripts/` плагіна.
  *Статус rc1:* verify.mjs ✓, frames.sh ✓, compress-budget.mjs ✓. Чотири шаблони
  сайт-стека (gen-pages / prerender / Telegram-lead / OSM-проєкція) — СВІДОМИЙ
  scope-cut: бойові еталони живуть у apps/smarts (eruhomist repo), портуються
  в плагін на першому використанні у новому проєкті (v1.1).
- Шаблони проєкту: `.award-re/` скелет (motion-score.md, FAILURES-LOG.md,
  CLIENT-BACKLOG.md, media-plan.md, design-intent.md, state/) створюється онбордингом.

## 2. КОПІ-ЯДРО (окремо, бо боліло найбільше)
- `skills/copywriting/` отримує: read-gate на 10 правил; `bans.yaml` (em-dash,
  «Невелика але власна», чужа/своя, «розумієш: твоє», Дивись/Бачиш, «без прикрас»,
  «незручні запитання», укр-антислоп) — розширюваний юзером; правило одного голосу
  ти/ви; протокол подачі = повні секвенції в чаті з назвою прийому і «чому працює»;
  ліміти (hero ≤6 слів, body ≤12); VI-регістр «дизайн-рішення → ефект», нуль питань.
- Мета-тест кожного рядка: «сказав би це живий продавець уголос?» + «чи можна
  поставити на будь-який ЖК?» (якщо так — викинути).

## 3. ЩО ЗБЕРІГАЄМО з v0.2 (не зносити)
design-grounding-хук (розмножити) · база знань (A1-критерії, VI_DNA, PB_performance,
композиційні лінзи, антислоп-каталоги) · порядок фаз build.md · ідею
variants-prototyping (доспечити в гейт) · eval-brain лічильні метрики ·
re-visual-search/interactive-map/forms-lead-capture (бойові) · культуру постмортемів ·
start-state hook + config.yaml.

## 4. ВІДЕО-КАМПАНІЯ: 10-15 еталонних сайтів (інструкція для юзера)
Формат запису (один сайт = один .mov, ім'я файлу = домен):
1. **Прохід 1 — драматургія (2-4 хв):** повільний рівномірний скрол згори донизу
   БЕЗ зупинок і кліків. Це дає тайминги, шви, theme-ритм, дистанції.
2. **Прохід 2 — інтеракції (2-5 хв):** меню (відкрити/закрити/перейти), всі ховери
   героїчних елементів, пікер квартир якщо є (ховер поверху → клік → план → юніт),
   карта, галереї/лайтбокси, форма (без сабміту), мобільне меню якщо знімаєш мобайл.
3. Бажано 1440p+, без DevTools у кадрі; якщо сайт має вау-момент — затримайся на
   ньому і повтори скрол туди-сюди 2 рази (видно scrub-механіку).
4. Скидаєш у ~/Downloads — я через /award-re:learn video-teardown перетворюю кожен
   на D_<site>.md + дельти граматики.

## 5. ЕТАПИ ВИКОНАННЯ
- **Етап 0 (фундамент):** CLAUDE.md → конституція v1 з чернетки; wiki-лінки → шляхи;
  розширення .award-re/ скелета. [S]
- **Етап 1 (граматика):** реструктуризація реєстру в таблицю T-ID; _GRAMMAR.md
  (формула шарів, партитура дистанцій, реєстр швів); переписати re-sections під стеки. [M]
- **Етап 2 (примус):** стейт-машина /section + state/*.yaml; 4 нові хуки;
  scripts/verify.mjs + frame-extract + compress-budget. [M]
- **Етап 3 (агенти v2):** контракти DoD для 4 агентів + нові media-director,
  teardown-anatomist. [M]
- **Етап 4 (копі+медіа ядра):** bans.yaml, read-gate, headline-DNA у copywriting;
  higgsfield-craft скіл з доктриною G + media-plan шаблон. [S-M]
- **Етап 5 (learn-loop):** команда /award-re:learn (failure / video-bug /
  video-teardown) + правило промоції провалів у закони. [M]
- **Етап 6 (відео-кампанія):** юзер записує 10-15 сайтів → ingestion кожного →
  збагачення граматики/реєстру новими T-ID і seam-патернами. [залежить від юзера]
- **Етап 7 (acceptance):** тест-бриф «фейковий ЖК» (дані+ассети в fixtures/);
  повний прогін пайплайна; критерії релізу v1.0.0: eval-brain ≥45/50,
  композиція ≥8/10, нуль банів у копі, всі гейти спрацювали (артефакти існують),
  прод-чек зелений. Якщо ні — постмортем і фікс до проходження. [M]

## 6. КРИТЕРІЙ УСПІХУ (визначення «як agronomichne з першого разу»)
Новий проєкт: онбординг → discovery → build БЕЗ ручних втручань юзера, крім
(а) відповідей на discovery-питання, (б) вибору з копі-секвенцій і прототипів,
(в) живого тесту з записом екрану. Перший аудит ≥45/50 і композиція ≥8/10
ДО будь-яких правок. Кількість ітерацій «юзер показує відео багу» ≤3 на сайт.
