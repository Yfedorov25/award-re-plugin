# award-re — КОНСТИТУЦІЯ v1 (закони для кожної команди, скіла, агента)

> 🔴 ПЕРЕД ПОБУДОВОЮ БУДЬ-ЯКОГО ЮНІТА: виконуй `skills/BUILD-PROTOCOL.md` ДОСЛІВНО.
> Живі величини референсів — `skills/teardowns/live-archive/README.md` (цитуй, не шукай заново).


> Плагін будує award-сайти НЕРУХОМОСТІ рівня Vide Infra. Усе нижче — ЗАКОНИ, не поради:
> кожен народився з реального провалу (F-номер = FAILURES-LOG smarts-agronomichne;
> К-номер = клас фейлів із жнив сесій, KAI/ingest/INGEST-FAILURES-CATALOG.md, 2026-07) або
> бойового рішення трьох сайтів (smarts/quadro/nahirna). Frontmatter — англійською,
> тіло — українською. ПРАВИЛО ПІДТРИМКИ: 2+ повтори провалу у FAILURES-LOG будь-якого
> проєкту = новий закон у цьому файлі.

## 0. ІДЕНТИЧНІСТЬ І МЕЖІ
- **Clone Vide Infra:** ERA/AIR/Springs/SilverPinewood/Ever = єдиний референс рівня й
  прийомів. Конкуренти клієнта = ТІЛЬКИ сенси (ЦА/болі/позиціонування), ніколи візуал.
  Жоден прийом не вигаданий — лише з реєстру/граматики (skills/grammar, _TECHNIQUE_REGISTRY).
- **NO WebGL (хард):** жодного Three.js/R3F. WebGL-прийоми VI → no-WebGL переклад
  (canvas-2D frame-scrub, inline-SVG, CSS, pre-rendered orbit). Текст завжди в DOM.
- **Стратегія перед дизайном:** discovery (research=сенси + Fedoriv-інтерв'ю +
  позиціонування + бренд + копі) → brief.md + brand.md → лише потім дизайн.
- **Decision hierarchy:** 1) інструкції юзера → 2) дані клієнта (brief/brand/config/info.txt)
  → 3) AI-припущення. Нема даних → блок ПРИХОВАНИЙ, не плейсхолдер і не вигадка.
- **Стек під задачу:** Next/React АБО vanilla+Vite+GSAP+Lenis АБО інше — константа лише
  рівень VI + NO WebGL.
- **Самодостатність:** у RE-флоу заборонено залежати від інших плагінів. Інструменти
  плагіна: Playwright MCP (DOM-факти, прод-чеки), Higgsfield MCP (генерації за розділом G),
  ffmpeg (кадри/пережим), sharp (композиція), vercel CLI (deploy + instant rollback),
  scripts/ цього плагіна. Стартовий хук чесно каже, чого бракує.
- **Техправила плагіна:** config.yaml = стан (mode/project_type/brand/arc/resume);
  skills невидимі (disable-model-invocation: true), команди READ-ають їх і виконують
  inline; модель у frontmatter не хардкодити; перед AskUserQuestion — порожній рядок;
  AskUserQuestion для ТЕКСТОВИХ варіантів заборонений (закон E10); тільки RE-ніша.
- **Чисті tool-блоки:** ніколи текст перед блоком виклику інструмента в одному
  повідомленні (виклик друкується текстом і не виконується); збій виклику →
  повторити блок ізольовано, «не ламатись на каунт». (К13)
- **Арт-дирекція кадру (урок Phoenix):** перед першим UI-файлом — re-art-direction:
  _ANTISLOP_design + _COMPOSITION_CRITIQUE → Design Intent на кожну секцію в
  .award-re/design-intent.md; design-grounding hook ХАРД-блокує UI-записи без нього.
  Хард-бани дизайну (потрібне явне «чому»): cream+orange, чисті #000/#fff,
  display без кирилиці, gradient-text, glassmorphism, центрування body,
  медіа-в-рамці-замість-full-bleed. eval-brain композиційна лінза <6/10 = не випускати.

## A. ПАЙПЛАЙН СЕКЦІЇ (порядок = закон)
- A1. Перед будь-яким рядком секції прочитай сирі дані клієнта (info.txt) і бери факти ТІЛЬКИ звідти, ніколи з попереднього копі. (F-02)
- A2. Зафіксуй motion-score (константи: один ease, один смузер, токени, reveal, parallax, theme-ритм) ДО побудови першої секції — section-builder будує строго за партитурою, не імпровізує.
- A3. Напиши Design Intent блок секції (Лінзи 1-5 + Squint/Full-bleed/Text-on-media/AI-Slop тести) ПЕРЕД CSS — без нього Write .html заборонений (блокуючий gate).
- A4. Якщо юзер дав референс-запис — зніми АРХІТЕКТУРУ секції покадрово (позиції/розміри/перекриття/глибини) перед кодом; косметика без геометрії = провал. (F-11)
- A5. Копі-варіанти подай повними секвенціями в чаті ДО прототипів; вибір — вільною відповіддю юзера. (F-06)
- A6. Збудуй 2-4 варіанти секції як single-file HTML-прототипи (/tmp/proto-*/, локальні порти) — юзер обирає; для hero прототип ОБОВ'ЯЗКОВИЙ (≥1 full-bleed).
- A7. Рішення юзера запиши в motion-score.md ОДРАЗУ після вибору, з датою і причиною відхилених варіантів.
- A8. Інтегруй і верифікуй DOM-фактами під живим Lenis: computed-геометрія текстів, overlap, стани анімацій. (F-12)
- A9. Перед хвилею змін — rollback-точка (git-тег + Vercel instant-rollback URL у ROLLBACK-*.md); хвиля = окремі коміти feat(wave).
- A10. Після деплою — прод-чек DOM-фактами всіх пунктів меню (відрендерений контент, не статус) і живий тест юзера із записом екрану. (F-26, F-27)
- A11. Кожен провал — у FAILURES-LOG.md того ж дня: що → чому → правило.
- A12. Перед скаффолдом секції перевір public/prototypes/ — якщо є затверджений прототип, він КОНТРАКТ (C17): переноси 1-в-1, не винаходь і не спрощуй. (towns F-16/F-18)

## B. ВЕРИФІКАЦІЯ
- B1. Серії скріншотів заборонені — вони брешуть (піни рвуть кадр, перекрите вікно морозить rAF) і палять токени; максимум 1 цільовий скрін.
- B2. Власна перевірка = DOM-факти: computed styles, геометрія, стани через evaluate.
- B3. Рантайм-чек сторінки = наявність відрендереного контенту, НІКОЛИ не HTTP-200/title. (F-26)
- B4. Прев'ю-сервер віддає index.html на всі маршрути — другорядні сторінки верифікуй лише на проді або сервері з реальними rewrites. (F-27)
- B5. Обидва шляхи даних окремо (event і storage-handoff), handoff — з повним перезавантаженням: hash-навігація не перезавантажує. (F-28)
- B6. Порожній catch на конверсійному шляху заборонений — мінімум console.error. (F-28)
- B7. Після lenis.stop() будь-який scrollTo мертвий — спершу lenis.start(). (F-28)
- B8. Firewall/bot-челендж ріже curl/headless: недоступність зонда ≠ зламаний сайт; фінальна істина = браузер юзера.
- B9. Перф міряй тільки на прод-білді (dev-HMR бреше: 31-34fps проти прод 58-73), Bottom-up self-time на виділеному вікні.
- B10. Headless не міряє scroll-feel — тач-плавність перевіряє реальний девайс юзера.
- B11. Фінальні стани анімацій страхуй setTimeout-ом: кастомний rAF не має фолбеку в фонових вкладках.
- B12. Після зміни схеми контент-даних — grep усіх споживачів ключа по src/ перед комітом. (F-26)
- B13. **Складні pin-сцени НЕ делегуй агентам як готовий продукт** — їхній звіт «працює в браузері» НЕ довіра (розрив звіт↔реальність росте зі складністю pin). Доводь САМ покадрово на 5 scroll-позиціях [0/.25/.5/.75/1], шукай ПОРОЖНЕЧІ й ОБРІЗИ; якщо агент будує прототип — оркестратор гортає кожен ПОВНІСТЮ перед показом юзеру. (towns F-21) [ТОП-урок]
- B14. Прийом іде в базу ТІЛЬКИ з гео-пробами в lab: bbox ключових елементів у межах viewport; пін-модуль після скролу 1.2vh → pinTop===0; sticky-тір offsetHeight ≤ innerHeight; одометр bottomSpread===0; карта/SVG — РАХУВАТИ кількість геометрії в DOM; інтерактив ДРАЙВИТИ (клік → семпл значень на 300/800/1500ms). «Вантажиться без errors» ≠ працює. (К1: 6 випадків у 4 сесіях) [ТОП-урок жнив]
- B15. Гейт не сміє брехати: скан від serve-кореня; ганяти КОЖЕН файл пулу включно з галереями/індексами; видимий текст ≠ мета-коментарі контракту; повнота звіряється проти ЯВНОГО переліку власника, не проти «того, що записав». False positive гейта = мета-фейл — вчить ігнорувати червоне. (К5)
- B16. Charset-гігієна видимого копі, промптів і доків: нуль em/en-dash, нуль сторонніх алфавітів, нуль форм «ти»; у JS-рядках для textContent — реальні символи, НІКОЛИ HTML-entities (рендеряться літерально). Прозі RECIPE і коментарям коду тире дозволене. (К7)
- B17. Числовий закон-ліміт фіксується ЛИШЕ після виміру метрики на 2-3 еталонах репо і явно пише, на що НЕ поширюється («мало підписаних POI» ≠ «порожня карта»: гейт рахує І лейбли, І геометрію). Гіперкорекція без калібрування дала «мільйон цяточок» → «чорну пустку» за один цикл. (К11)

## C. КОМПОЗИЦІЯ / ДРАМАТУРГІЯ
- C1. Flagship-секція = 5-7 прийомів НАШАРОВАНИХ в одному кадрі, ніколи «1 прийом + дрібниці». (МЕТА-ПАТЕРН)
- C2. Колаж, не слайдшоу: елементи співіснують на глибинах; counter-степпери «02/03» і прогрес-бари заборонені. (F-11)
- C3. До кожної секції 3 питання: дистанція камери і чи відрізняється від сусідніх; що глядач бачить утретє; що перетікає далі.
- C4. Повний план обʼєкта = пуант, максимум 2 рази на сторінку.
- C5. Одна ідея = одне місце: секцію-дубль видаляй, не прикрашай.
- C6. Пін-бюджет = 1-2 на сайт (3 — задокументована стеля); одна секція = один pin; чужі системи не накладають свій pin. (F-13)
- C7. Мобайл за замовчуванням не пінить (desktop-gate); виняток лише syncTouch:true + syncTouchLerp 0.1 + data-lenis-prevent.
- C8. Theme-flip = базовий розділювач; чергуй heavy/light, ніколи 2 важкі поспіль.
- C9. Act-слова — максимум 1-2 на сайт, clip-маска inset 100%→0 за 0.8-1.2s.
- C10. Шов = естафета (лінія/колір/елемент/sticky-under перетікає); спосіб переходу не повторюється двічі.
- C11. Full-bleed = дефолт; текст впечений у медіа зі scrim; «асет у океані порожнечі» = Phoenix-провал.
- C12. Нуль квадратів: пілюлі/овали; прямокутні ghost-кнопки заборонені, CTA кругла зі стрілкою.
- C13. Hover-інфо у visual-search = постійні пілюлі-якорі + закріплений попавер (Popper, .4s); курсорні тултіпи для конверсійних даних — ніколи. (F-25)
- C14. Хедер = [меню] · лого · [одна тиха CTA].
- C15. Бракує дистанцій — згенеруй чесні макро матеріалів і людські моменти без облич.
- C16. Накладання на реальне фото калібруй ruler-overlay математикою, не «на око».
- C17. **Затверджений прототип = КОНТРАКТ**: переноси розмітку+CSS+JS 1-в-1, не переписуй з пам'яті; звір фінал з прототипом по списку прийомів (відео? хореографія титрів? letterbox? scrim?); існуючий ассет (відео) МУСИТЬ бути під'єднаний. (towns F-16) [ТОП-урок: 3 повтори F-01/16/18]
- C18. Переходи/слайси = ТІЛЬКИ механіки із затверджених прототипів; перед «новим прийомом» спитай чи це вже є в strip-прототипах → бери 1-в-1, не винаходь нове. (towns F-18)
- C19. Слайс/шов працює на ДВОХ рівнях: локально в секції ТА як перехід МІЖ секціями; text-breath секції (§02/§08) теж отримують слайс-шов + акт-reveal, не «нуль прийомів». Посилює C10. (towns F-17)
- C20. Важливі секції — НЕ «reveal все одразу»: pin/scrub, елементи заходять ПО ОДНОМУ по progress, паралакс різної швидкості, один меседж за такт. (towns F-19)
- C21. Прогрес високої секції (>1 екран) = РЕАЛЬНИЙ прохід крізь в'юпорт: `p=(-rect.top)/(rect.height - vh*K)`, не top-anchor (стискає розкриття у перші 30% = «розкрито наперед»). (towns F-24)
- C22. Фонові гіганти-цифри/слова = ШУМ, не глибина; службові лічильники «N/6» заборонені; фонове медіа — чистий full-bleed або нема (не напівпрозора маска-пляма). ghost-цифра в L2 — ТОНКИЙ шар, не гігант-фон. (towns F-20)
- C23. Flagship/контент-секція МУСИТЬ мати L1-медіа (реальний рендер/фото); текст-на-кольорі заборонено. Squint: прибери текст → лишається кінематографічний кадр? Ні = інфографіка. design-intent НЕ забороняє медіа-глибину, якщо motion-score її вимагає (конфлікт → на користь motion-score). Посилює C11. (towns F-23)
- C24. «Менше скролу» ≠ «менше шарів»: скорочуй ДИСТАНЦІЮ (коротший pin/scrub), не ГЛИБИНУ (повний стек шарів лишається). (towns F-23)
- C25. Кожен фіксований текст над змінними full-bleed фото має ВЛАСНИЙ локальний скрим; squint-тест на НАЙСВІТЛІШОМУ кадрі (не темному). Посилює C11. (towns F-22)
- C26. ЗАКОН РОЛІ: закони секції виводяться з її РОЛІ, не копіюються з сусідньої (фінал-CTA = ОДИН екран, не 3-акт; датасет без чисел ≠ числовий хребет). Кожна доктрина «секція володіє X» — одразу з контрприкладом: «X ≠ заміна сцени; герой = медіа, X = акцент». (К2: 5 випадків у 3 сесіях)
- C27. Перед показом юзеру L1-композиція проходить award-рубрику A1 ЧИСЛАМИ (тип-контраст ≥4:1, 8pt-сітка, асиметрія, негативний трекінг великих кеглів); «функціонально працює» ≠ готово до показу; планка = НАЙКРАЩІ минулі секції, не середні. (К9: «наче джуніор дизайнер»)
- C28. bbox-перетин декору/ховер-карток із контентним об'єктом = 0; тест ЗАВЖДИ на СЕРЕДНЬОМУ елементі ряду, не на крайньому. (К10)

## D. РУХ / ПЕРФ
- D1. ОДИН смузер: Lenis lerp 0.08-0.12 + scrub:true; стек lerp+scrub+rAF-лерп — видаляй шари.
- D2. НІКОЛИ mix-blend/backdrop-filter над канвасом/скрабленою поверхнею; заміна = щільніший tint.
- D3. НІКОЛИ CSS transition на властивості, керованій scroll-змінною.
- D4. Анімуй тільки transform/opacity.
- D5. will-change знімай після one-shot; постійний = Layerize-податок.
- D6. Частотні твіни через gsap.quickTo; getComputedStyle у кадрі заборонений; лічильники пишуть DOM лише при зміні.
- D7. Кадри секвенцій — immutable headers; .vercelignore-патерни якорити (/assets/, не assets/).
- D8. Decode-вікно ≥ лаг смузера; DPR cap 1.5; requestIdleCallback голодує під тікером — priority-window.
- D9. Вічний rAF, що пише в DOM щокадру — гейтуй на «чи є що анімувати».
- D10. <main> не сміє бути порожнім до JS: prerender (SSR/postbuild), одне джерело розмітки.
- D11. Ease — один на проєкт і обирається ПІД проєкт (per-site, не глобальна константа).
- D12. Split по словах/рядках (стаггер 60ms), не по літерах; max-width у ch; fonts.ready перед SplitText. (F-12)
- D13. Не покладайся на gsap.utils.parseEase — інлайнь ease. (F-14)
- D14. Перший кадр сторінки керується інлайн-скриптом у <head>. (F-15)
- D15. Pin на Lenis = pinType:'transform' + anticipatePin:1; scroll-snap для RE-покупця заборонений.
- D16. Після FLIP/перемикання режиму — ScrollTrigger.refresh().
- D17. Шрифти self-host woff2 + preload; лерп = лаг, дистанція = швидкість (різні важелі).

## E. КОПІ
- E1. Нуль em-dash у видимому копі, включно з og/twitter/тайтлами — сепаратор « · ».
- E2. Один голос на сайт (ти АБО ви); виняток /privacy.
- E3. Бани власника: «Невелика, але власна»; чужа/своя; «розумієш: твоє»; парцеляція-драма; «Дивись/Бачиш»; «незручні запитання».
- E4. Кожен рядок = конкретний образ або проста цифра; тест «сказав би живий продавець уголос?». (F-01)
- E5. НІКОЛИ не вигадуй цитати/факти від названих людей чи компаній. (F-02)
- E6. Не приписуй читачу вік/історію/статус. (F-03)
- E7. Для недобудованого — тільки майбутній/умовний час. (F-04)
- E8. Укр-антислоп прогін: русизми, неіснуючі слова, банлист кліше. (F-05)
- E9. VI-регістр = декларативні тези «дизайн-рішення → ефект», нуль питань і гіпербол.
- E10. Варіанти — повні секвенції в чаті з назвою прийому; AskUserQuestion для текстів заборонений. (F-06)
- E11. Ліміти: hero ≤6 слів, H1 3-6, body-речення ≤12; одна думка = одне речення.
- E12. Перед першим рядком копірайтер читає всі 10 копі-правил кореня.
- E13. CTA не тисне: одна тиха дія.
- E14. ДНК хедлайнів: ~70% прості/описові; гра слів ≤1 на сайт і лише в hero.
- E15. Будь-яке видиме копі — ТІЛЬКИ через copy-fedoriv агента (читає brand.md+brief.md+voice-foundations+_ANTISLOP); orchestrator НІКОЛИ не пише фінальний копі з голови. Без agent-antislop-self-check (em-dash 0, кліше 0, «ви», акт без крапки) копі не йде в інтеграцію. (towns F-06)
- E16. КОПІ-ДО-БІЛДА: видиме копі затверджується ДО білда; білдеру йдуть ДОСЛІВНІ затверджені рядки; «нуль поезії» + «на ви» + бан-лист вставляються ДОСЛІВНО в кожну версію промпта, не посиланням — закон у пам'яті двічі кусав повторно, тримає тільки гейт. (К3)

## F. ЧЕСНІСТЬ ДАНИХ І МЕДІА
- F1. Непідтверджений факт → блок прихований/помʼякшений, ніколи не вигаданий.
- F2. Число = одне канонічне джерело; розбіжності даних клієнта — у беклог, не «середнє».
- F3. Ціни точні, без округлення вгору.
- F4. Демо-статуси явно позначай і питай реальні.
- F5. Недобудоване показуй процесом будівництва, не готовим у часових станах.
- F6. Вигадані візуалізації — disclosure-пілюлі «приклад».
- F7. Без орендних ставок у фактажі — жодних обіцянок дохідності.
- F8. Карта = проекція реальних даних (OSM, Дейкстра, час-кінетика); «на око» заборонено. (F-24)
- F9. Генератор = source of truth (сторінки/розмітка/карта зі скрипта, правки в генератор).
- F10. Юридична перевірка claims (берегова смуга, «єдиний», власність, строки).
- F11. Операційні обіцянки — лише після підтвердження власником.
- F12. Вид з вікна/двору = РЕАЛЬНИЙ масштаб ділянки з генплану; не показувати більше землі, ніж є (рендер «існує», але бреше про масштаб). (towns F-09)
- F13. Оточення відтворювати ЛИШЕ з оригінальних рендерів об'єкта — переглянути ВСІ перед генерацією, нічого не вигадувати (газон/сад/дерева, яких нема). (towns F-10)
- F14. Документо-подібний артефакт у демо-даних несе обов'язкове поле invented-placeholder: true/false — вигадані реквізити в прийнятому темплейті це false-done, який не ловить жоден скан. Гео-точки валідувати зворотною відстанню до відомого об'єкта; мережевий заклад — тільки з адресою. (К6)
- F15. Конфлікт канонічних чисел = РІШЕННЯ ВЛАСНИКА: ескалювати ОДИН раз, канон зафіксувати письмово (memory/config/info.txt), не тягти прапорець між сесіями (smarts 20000/22800/22572 тягнувся 3 сесії). Розширює F2. (К6)

## G. АССЕТИ (Higgsfield + локальна обробка)
- G1. Фото — тільки 4K; день→ніч = relight одним кроком від реального денного кадру; «change ONLY lighting» + заборони.
- G2. Перед генерацією матеріалу — кроп реального з рендера як референс + color-lock; після — звірка кольору і сусідства. (F-23)
- G3. i2i: дослівна геометрія + «keep identical» + заборони; результат звіряти покадрово. (F-21)
- G4. Відео день→ніч = i2v зі start+end image (end = затверджена ніч того ж кадру).
- G5. Рух камери пояснюється кадром (anti-morph); cinemagraph-штори заборонені. Рух, що ВІДКРИВАЄ нову площу / обліт ВЗДОВЖ забудови = заборона (домальовує вигадані блоки: towns обліт, nahirna D10 фантомний будинок, Veo aerial — 3 повтори); мікро-push-in лише за Законом-11 (_MEDIA_QA_CHECKLIST: спроба + покадровий frame-QA + фолбек pan+zoom по 4K). (К-media)
- G6. Veo-хвіст пливе — 6s або обрізати ffmpeg; кліпи не лупляться naive.
- G7. Рамка = aspect-ratio рівно під кадр, пропорцію РАХУЙ; cover-кроп малих інтерʼєрів заборонений. (F-22)
- G8. Макро-дистанція = чесні кропи з 4K-мастерів; мастери поза public/ (gitignored).
- G9. Пережим з бюджетами обовʼязковий; перевірка ваги після кожної партії.
- G10. Кожна генерація на рев'ю парою РЕЗУЛЬТАТ+РЕФЕРЕНС у ~/Downloads.
- G11. Кириличний текст у генераціях не виходить — композиція локально (sharp).
- G12. Кредити фіксуй проти cap.
- G13. Пласкі текстури-зразки заборонені — матеріал деталлю НА обʼєкті. (F-20)
- G14. **Ніч ≠ затемнений день.** Нічний relight МУСИТЬ явно: (1) «remove ALL sunlight/sun rays/bright light pools on floor+furniture — no sun at night»; (2) «scene DARK, low exposure»; (3) «light ONLY from interior lamps + LED cove — warm pools AROUND sources, rest to shadow»; (4) night-ref як стиль-якір. Без 4 пунктів = фейк. (towns F-25)
- G15. Стан штучного світла протилежний по часу: ДЕНЬ-промпт «daylight is ONLY light; LED cove + all artificial OFF/unlit; no glowing strips»; НІЧ вмикає їх (G14). (towns F-26)
- G16. **Object-inventory LOCKED день↔ніч.** Ніч НЕ додає/прибирає предмети: «do NOT add lamps/pendants/chandeliers not in reference; only EXISTING fixtures emit light; every object identical to day». «lamps ON» ≠ дозвіл ДОДАТИ. (towns F-27)
- G17. Ніч ЗАВЖДИ i2i з ФІНАЛЬНОГО денного кадру (не з оригіналу окремо) → геометрія ідентична, crossfade без зсуву. Розширює G4 на статичні пари. (towns F-25/26)
- G18. Анти-diptych у text→image інтер'єрах: «single seamless photo, NOT diptych, no vertical seam, no split panels». (towns F-26)
- G19. Камера враховує ПОВЕРХ: 1-й = вид на рівні землі (тераса прямо за склом); 2-й = «elevated view looking slightly DOWN onto terrace roof/yard below, horizon higher». (towns F-14)
- G20. Планувальний/інтер'єр-рендер = i2i з РЕАЛЬНОГО плану клієнта, не «з голови»: знайти PDF → растер PyMuPDF (`import fitz`, НЕ poppler) → кроп креслення → media_upload → i2i з описом геометрії (де кухня/стіл/сходи/вікна/вихід). (towns F-08)
- G21. Звірка вікон/прорізів = за ЧИСЛАМИ розмірних ланцюжків плану (мм), не на око: «380 3120 120 2380 380» = 2 прорізи; перевіряй кількість/ширину/позицію/двері. (towns F-11)
- G22. Прив'язка осей плану ПЕРЕД генерацією кімнати: де вхід/заїзд (перед), де вихід на терасу (зад); перенести на 2-й поверх; кожна кімната — вікно на яку вісь і що там (двір/вулиця). (towns F-13)
- G23. Планка інтер'єр-рендерів = A2+ Design Studio: hidden LED cove/perimeter glow, soft sun patches, tactile linen+ceramics, чорні акценти (slim floor lamp, cone pendant, чорні стільці), cinematic depth, lived-in не staged. Style-ref i2i. (towns F-12)
- G24. QC-агент звіряє КОЖЕН елемент рендера з планом ПІСЛЯ кожної генерації (око пропускає): кількість вікон/простінків (генератор робить curtain-wall замість 2 вікон), кількість приладів (додає зайве), підвіконня (склить до підлоги). (towns F-15)
- G25. Сирий 3D-рендер НІКОЛИ не keyframe для відео — keyframe тільки затверджений Higgsfield job-id (towns hero + kling-never-raw-3d-keyframes: 2 повтори). (К-media)

## H. ПРОЦЕС
- H1. FAILURES-LOG.md append-only, того ж дня.
- H2. motion-score = ФАКТ: після кожної хвилі звіряй із кодом, усувай дрейф.
- H3. CLIENT-BACKLOG.md обовʼязковий: дані-блокери + дизайн-хвилі під прототипи.
- H4. Перед хвилею — git-тег stable-pre-* + Vercel rollback-URL; після ролбеку спершу «що саме не зайшло».
- H5. Розгалуження — по наміру покупця (self/invest), show/hide + sessionStorage.
- H6. Контракти даних незмінні при редизайні UI.
- H7. Після зміни схеми — grep споживачів до коміту. (F-26)
- H8. Trade-off з реальними ставками — через council, не сольно.
- H9. Деплой тільки npm run build (prebuild-хуки); rewrites extensionless при cleanUrls.
- H10. let-стейт оголошуй ДО хелперів, що його читають (TDZ + тихий catch = невидимий краш). (F-28)
- H11. Під-системи не конфліктують: seams/chrome не чіпають чужі секції. (F-13)
- H12. «Правильні інгредієнти ≠ award»: art-direction gate — блокуючий, не чек-лист.
- H13. СТАТУС ПРОТИ КАНОНУ: стан секцій у хендофі = таблиця проти SECTIONS.yaml з точним шляхом «де в базі»; ніколи «~4 з ~10»; готове НІКОЛИ не називати «наступним»; нова сесія перед дією звіряє реальний стан файлів/remote, не вірить хендофу. (К8)
- H14. ЕХО-СПИСОК: двозначне заперечення про включення/виключення («а до 11 теж не записуємо») = стоп-сигнал; полярність НЕ вгадувати — перелічити власнику фінальний список назад і чекати підтвердження переліком. (К12)
- H15. ТЕМПЛЕЙТ-ФРЕЙМ: база будує ТЕМПЛЕЙТИ для перевикористання між сайтами (4-5 варіантів на секцію для міксування); дані/рендери поточних проєктів = лише приклад наповнення, не ціль. (жнива №1/№9)
- H16. Деплой бази/галерей = vercel preview + alias, НІКОЛИ --prod; verify-скрипти й deploy-папки живуть у git, не в /tmp — ефемерні артефакти = невідтворюваність. (жнива П4)
- H17. Одна система імен бібліотека↔реєстр: кожен official атом/комбо несе source.registry_ref (T-ID з _REGISTRY_TID або "x:<чому поза реєстром>"); порожній ref робить гап-аналіз сліпим — 30/58 рецептів без міток ховали 21/26 невідтворених AIR-прийомів. (AIR-harvest 2026-07)

## I. РОБОТА З ВІДЕО ЮЗЕРА
- I1. Запис екрану юзера = канонічне джерело багів і референсів.
- I2. Розбір = ffmpeg fps-кадри → контактні листи → діагноз час/секція.
- I3. Тірдаун чужого сайту з відео → мастер-документ аудиту → план перебудови.
- I4. З референс-запису знімай ГРАМАТИКУ (співіснування елементів, глибини, тайминги) у motion-score ДО коду. (F-11)
- I5. Власні генерації теж frame-QA перед вживанням.

## J. CONTROL (єдине джерело правди)
Крос-проєктний шар `control/` у корені монорепо: `projects.yaml` (реєстр об'єктів) +
`tasks/T-NNNN.md` (задачі) + БД (ліди/метрики). Дашборд `apps/control` його читає.
Усі агенти ділять цей стан — не тримають його «в голові».
- J1. Перед роботою над об'єктом — прочитай `control/projects.yaml` + його активні задачі
  (`grep "project: <id>" control/tasks/*.md`). Стан бери звідси, не вигадуй.
- J2. Зробив значущу зміну → онови відповідну `control/tasks/T-*.md`: стадія + рядок у Лозі з датою.
- J3. **`demo-pending → done` ЛИШЕ після рядка `demo-confirm:` у Лозі** (демо з девелопером/юзером).
  Агент НІКОЛИ не ставить `done` сам. Це гейт; дашборд підсвічує порушення.
- J4. `platform: mobile` задача СТАРТУЄ `stage: backlog` («not-started»), поки нема явного старту.
- J5. Нова задача з чату → команда `/award-re:task` (кристалізація у файл), не «в голові» (інакше губиться).

## K. ЗОВНІШНІ БІЛДЕРИ (Claude Design тощо)
- K1. НІКОЛИ НАТХНЕННЯ, ЗАВЖДИ КОД: зовнішній білдер отримує (а) повний арсенал атомів З КОДОМ у fenced-бандлах, (б) reference-combos як граматику збірки, (в) реєстр прийомів поіменно в брифі. Опис словами → двійники з вигаданими координатами і чужою типографікою. (К4, F-57)
- K2. Порт із зовнішнього білдера верифікується ПО-ЧИСЛОВО: grep сигнатур атомів; порівняння tuned-значень із component.js; atoms_used ≥3; house-шрифти + house-ease. (F-57)
- K3. Гібрид-межа: зовнішній ШІ робить КОМПОЗИЦІЮ; порт на наші движки — наш; core-атоми НІКОЛИ не форкати, re-skin лише CSS-токенами. (S-FLOOR)
- K4. Субагентам і зовнішнім сесіям — уже ЛОКАЛЬНІ файли: DesignSync/remote-auth працює тільки в головній сесії. (К13, F-43)
